# Dependências de produção — versões fixadas às validadas neste projeto.
# Gerado a partir do ambiente de desenvolvimento, curado para incluir
# apenas o que a aplicação importa (não ferramentas de lint/teste —
# essas vivem em requirements-dev.txt).

# -- Web framework --
fastapi[standard]==0.139.2
uvicorn==0.51.0
starlette==1.3.1
python-multipart==0.0.32

# -- Base de dados --
sqlalchemy[asyncio]==2.0.51
aiosqlite==0.22.1
asyncpg==0.31.0
alembic==1.18.5
greenlet==3.3.2

# -- Validação/configuração --
pydantic==2.13.4
pydantic-settings==2.14.2

# -- Autenticação --
bcrypt==5.0.0
PyJWT==2.7.0

# -- Filas assíncronas + cache --
celery==5.6.3
redis==8.0.1

# -- Machine Learning --
numpy==2.4.4
scipy==1.17.1
scikit-learn==1.8.0
xgboost==3.3.0
mlflow==3.14.0
joblib==1.5.3

# -- Observabilidade --
structlog==26.1.0
prometheus-client==0.26.0
prometheus-fastapi-instrumentator==8.1.0
